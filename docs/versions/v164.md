# v164 作業完了記録

このセッション（2026-04-07）で実施した修正の最終記録。

---

## 完了済み作業

### 1. バージョン文字列の修正（3箇所すべて）

v164のバージョンドキュメントは生成済みだったが、`index.html` 内のバージョン文字列が v163 のままだったため修正。

| 行 | 箇所 | 修正前 → 修正後 |
|----|------|-----------------|
| 13行 | `<title>` | `夜語り v163` → `夜語り v164` |
| 246行 | トップバー `<span class="ver">` | `v163` → `v164` |
| 595行 | `APP_VER` 定数 | `'v163'` → `'v164'` |

### 2. PWAアイコンの再生成

前バージョン（v163）で生成されたアイコンが事実上ほぼ真っ黒（726バイト / 2049バイト）で機能していなかったため再生成。

| ファイル | 旧サイズ | 新サイズ | 内容 |
|----------|----------|----------|------|
| icon-192.png | 726バイト | 7,108バイト | 夜空・三日月・星・「夜」文字（ゴールド系） |
| icon-512.png | 2,049バイト | 21,261バイト | 同上（512px版） |

### 3. Google Cloud TTS APIキーのURL露出を完全修正

前セッションで `fetchGoogleAudio` は修正済みだったが、**ListVoices（ボイス一覧取得）のXHRリクエストが未修正のままだった**ことを発見・修正。

| 処理 | 修正前 | 修正後 |
|------|--------|--------|
| `fetchGoogleAudio` (fetch) | 前セッション修正済み ✅ | `x-goog-api-key` ヘッダー |
| ListVoices XHR (`saveApiKey`内) | `?key=xxxxx` クエリパラメータ ❌ | `x-goog-api-key` ヘッダー ✅ |

これにより、APIキーがURLに露出する経路がすべて塞がれた。

### 4. CLAUDE.md バージョン管理プロトコルの改善

| 変更 | 内容 |
|------|------|
| 実行手順にステップ4を追加 | `index.html` の3箇所を vXXX に書き換える手順を明記 |
| 3箇所の明記 | `<title>` / `.ver` スパン / `APP_VER` 定数 |
| 重複ルールの整理 | 「機能追加時の注意点」の重複記述を削除しプロトコルへ一本化 |

---

## 確認済みの問題なし

- v164の各修正（履歴読み込みエラー修正・addUsageバグ・Chirp3対応・Canvas高DPI・CPM改善等）にハルシネーション・既存機能破壊は確認されなかった
